<!DOCTYPE html>

<html lang="en">
<head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1.0" name="viewport"/>
<title>ABSOLUTE AiDOL</title>
<meta content="ABSOLUTE AiDOL — indie J-Pop idol &amp; content creator based in Seattle." name="description"/>
<link href="https://fonts.googleapis.com" rel="preconnect"/>
<link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect"/>
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@500;700;800&amp;family=Quicksand:wght@400;500;600;700&amp;display=swap" rel="stylesheet"/>
<style>
  :root{
    --blush:#FFF3F5;
    --white:#FFFFFF;
    --crimson:#FF3355;
    --crimson-deep:#E31B41;
    --wine:#3A0F16;
    --petal:#FFD3DE;
    --petal-soft:#FFE8ED;
    --halo:#F6C976;
    --shadow: 0 12px 30px rgba(58,15,22,0.12);
  }

  *{box-sizing:border-box;}
  html{scroll-behavior:smooth;}
  body{
    margin:0;
    background:var(--blush);
    color:var(--wine);
    font-family:'Quicksand', sans-serif;
    font-weight:500;
    overflow-x:hidden;
  }
  h1,h2,h3,.display{
    font-family:'Baloo 2', cursive;
    font-weight:800;
    color:var(--crimson);
    margin:0 0 .4em 0;
  }
  a{color:var(--crimson-deep);}
  img{max-width:100%; display:block;}
  ::selection{background:var(--petal); color:var(--wine);}

  :focus-visible{
    outline:3px solid var(--halo);
    outline-offset:3px;
    border-radius:6px;
  }

  /* ---------- ambient sparkles ---------- */
  .sparkle-field{
    position:fixed; inset:0; pointer-events:none; z-index:0; overflow:hidden;
  }
  .sparkle{
    position:absolute;
    bottom:-40px;
    font-size:1.1rem;
    opacity:.55;
    animation: drift 16s linear infinite;
    color:var(--halo);
  }
  @keyframes drift{
    0%{ transform:translateY(0) translateX(0) rotate(0deg); opacity:0; }
    10%{ opacity:.6; }
    90%{ opacity:.6; }
    100%{ transform:translateY(-115vh) translateX(30px) rotate(180deg); opacity:0; }
  }

  /* ---------- nav ---------- */
  header{
    position:sticky; top:0; z-index:50;
    background:rgba(255,243,245,0.9);
    backdrop-filter: blur(8px);
    border-bottom:1px solid var(--petal);
  }
  .nav-wrap{
    max-width:1080px; margin:0 auto;
    display:flex; align-items:center; justify-content:space-between;
    padding:.7rem 1.2rem; gap:1rem;
  }
  .brand{
    font-family:'Baloo 2', cursive; font-weight:800;
    color:var(--crimson); font-size:1.3rem; text-decoration:none;
    display:flex; align-items:center; gap:.5rem;
    white-space:nowrap;
  }
  .brand .brand-logo{
    height:38px; width:auto;
  }
  nav{
    display:flex; flex-wrap:wrap; gap:.35rem;
    justify-content:flex-end;
  }
  nav button{
    font-family:'Quicksand', sans-serif;
    font-weight:700; font-size:.82rem;
    letter-spacing:.02em;
    background:transparent; border:none; cursor:pointer;
    color:var(--wine); padding:.5rem .8rem; border-radius:999px;
    position:relative;
    transition: color .2s ease, background .2s ease;
  }
  nav button:hover{ background:var(--petal-soft); }
  nav button.active{
    background:var(--crimson); color:#fff;
  }
  nav button.active::after{
    content:"♡";
    margin-left:.35rem;
    font-size:.75em;
  }

  /* ---------- layout ---------- */
  main{ position:relative; z-index:1; }
  .panel{ display:none; animation: fadeUp .5s ease both; }
  .panel.active{ display:block; }
  @keyframes fadeUp{
    from{ opacity:0; transform:translateY(14px); }
    to{ opacity:1; transform:translateY(0); }
  }
  .wrap{ max-width:1080px; margin:0 auto; padding:3rem 1.4rem 5rem; }
  .eyebrow{
    text-transform:uppercase; letter-spacing:.18em; font-size:.72rem;
    font-weight:700; color:var(--crimson-deep); opacity:.75;
  }
  .lede{ font-size:1.05rem; line-height:1.7; max-width:60ch; }
  .btn{
    display:inline-flex; align-items:center; gap:.5rem;
    background:var(--crimson); color:#fff; text-decoration:none;
    font-family:'Baloo 2',cursive; font-weight:700; font-size:.95rem;
    padding:.75rem 1.4rem; border-radius:999px;
    box-shadow:var(--shadow);
    transition: transform .15s ease, background .15s ease;
  }
  .btn:hover{ transform:translateY(-2px); background:var(--crimson-deep); }
  .btn.ghost{
    background:#fff; color:var(--crimson-deep); border:2px solid var(--petal);
  }
  .btn.ghost:hover{ background:var(--petal-soft); }

  /* ---------- halo photo frame (signature element) ---------- */
  .halo-frame{
    position:relative;
    display:inline-block;
    border-radius:50%;
    padding:10px;
  }
  .halo-frame::before{
    content:"";
    position:absolute; inset:-6px;
    border-radius:50%;
    background: conic-gradient(from 0deg, var(--halo), var(--petal), var(--crimson), var(--halo));
    opacity:.55;
    filter: blur(1px);
    animation: spin 14s linear infinite;
  }
  .halo-frame::after{
    content:"";
    position:absolute; inset:0;
    border-radius:50%;
    background:var(--blush);
  }
  .halo-frame img{
    position:relative; z-index:1;
    border-radius:50%;
    aspect-ratio:1/1;
    object-fit:cover;
    width:100%;
    box-shadow: inset 0 0 0 6px #fff;
  }
  @keyframes spin{ to{ transform:rotate(360deg); } }

  /* ---------- cute wings flanking framed photos ---------- */
  .winged-frame{ position:relative; display:inline-block; }
  .winged-frame::before, .winged-frame::after{
    content:"";
    position:absolute; top:36%; width:32%; aspect-ratio:1.6/1;
    background-image:var(--wing-image);background-repeat:no-repeat;background-position:center;background-size:contain;
    z-index:4; pointer-events:none;
    filter: drop-shadow(0 6px 10px rgba(58,15,22,.16));
  }
  .winged-frame::before{
    left:-13%; transform:scaleX(-1) rotate(8deg);
    animation: wingflap-l 4s ease-in-out infinite;
  }
  .winged-frame::after{
    right:-13%; transform:rotate(-8deg);
    animation: wingflap-r 4s ease-in-out infinite; animation-delay:.4s;
  }
  @keyframes wingflap-l{
    0%,100%{ transform:scaleX(-1) rotate(8deg); }
    50%{ transform:scaleX(-1) rotate(2deg); }
  }
  @keyframes wingflap-r{
    0%,100%{ transform:rotate(-8deg); }
    50%{ transform:rotate(-2deg); }
  }

  /* ---------- HOME ---------- */
  .home-hero{
    display:grid; grid-template-columns: 1.1fr 1fr; gap:3rem; align-items:center;
    padding-top:1rem;
  }
  .hero-photo-wrap{ position:relative; min-height:420px; }
  .hero-photo-wrap .halo-frame{ width:min(340px, 80vw); }
  .float-polaroid{
    position:absolute;
    background:#fff; padding:8px 8px 22px;
    border-radius:6px;
    box-shadow:0 14px 24px rgba(58,15,22,.18);
    width:120px;
    animation: bob 7s ease-in-out infinite;
  }
  .float-polaroid img{ border-radius:2px; aspect-ratio:3/4; object-fit:cover; }
  .p1{ top:-10px; right:0; transform:rotate(8deg); animation-delay:0s; }
  .p2{ bottom:10px; right:40px; transform:rotate(-10deg); width:100px; animation-delay:1.4s; }
  .p3{ bottom:-30px; left:10px; transform:rotate(6deg); width:110px; animation-delay:2.6s; }
  @keyframes bob{
    0%,100%{ transform-origin:center; translate:0 0; }
    50%{ translate:0 -14px; }
  }
  .home-copy .name{ font-size:2.6rem; line-height:1.05; }
  .home-copy .tagline{ font-size:1.05rem; color:var(--wine); opacity:.85; margin-bottom:1.4rem; }
  .booking-line{
    margin-top:2rem; padding:1rem 1.2rem; background:#fff; border-radius:14px;
    border:1.5px dashed var(--petal); font-size:.92rem;
  }
  .booking-line a{ font-weight:700; }

  @media (max-width: 820px){
    .home-hero{ grid-template-columns:1fr; }
    .hero-photo-wrap{ min-height:340px; margin:0 auto; }
    .float-polaroid{ display:none; }
  }

  /* ---------- SOCIALS ---------- */
  .social-grid{
    display:grid; grid-template-columns:repeat(auto-fit, minmax(200px,1fr));
    gap:1rem; margin-top:2rem;
  }
  .social-card{
    background:#fff; border-radius:16px; padding:1.4rem;
    text-decoration:none; color:var(--wine);
    box-shadow:var(--shadow);
    display:flex; align-items:center; gap:.8rem;
    font-family:'Baloo 2',cursive; font-weight:700;
    transition: transform .15s ease;
    border:1.5px solid transparent;
  }
  .social-card:hover{ transform:translateY(-3px); border-color:var(--petal); }
  .social-card .ic{ font-size:1.5rem; }
  .socials-main{
    margin-top:2.5rem; text-align:center; background:#fff; border-radius:20px;
    padding:2.4rem 1.5rem; box-shadow:var(--shadow);
  }

  /* ---------- ARTIST ---------- */
  .artist-hero{ display:grid; grid-template-columns:.9fr 1.3fr; gap:3rem; align-items:center; }
  .artist-hero .halo-frame{ width:min(300px,80vw); margin:0 auto; }
  .gallery{
    margin-top:3rem;
    display:grid; grid-template-columns:repeat(auto-fit, minmax(200px,1fr)); gap:1rem;
  }
  .gallery img{
    border-radius:16px; aspect-ratio:4/5; object-fit:cover;
    box-shadow:var(--shadow);
    transition: transform .2s ease;
  }
  .gallery img:hover{ transform:scale(1.02) rotate(-.5deg); }
  @media (max-width:820px){ .artist-hero{ grid-template-columns:1fr; } }

  /* ---------- MUSIC ---------- */
  .music-card{
    background:#fff; border-radius:24px; box-shadow:var(--shadow);
    display:grid; grid-template-columns:340px 1fr; overflow:hidden;
  }
  .music-cover{ position:relative; background:var(--petal-soft); display:flex; align-items:center; justify-content:center; padding:2rem;}
  .music-cover img{ border-radius:12px; box-shadow:0 10px 26px rgba(58,15,22,.25); }
  .music-info{ padding:2.4rem; }
  .single-title{ font-size:2rem; margin-bottom:.2rem; }
  .release-pill{
    display:inline-block; background:var(--petal-soft); color:var(--crimson-deep);
    font-weight:700; font-size:.8rem; padding:.35rem .8rem; border-radius:999px;
    margin-bottom:1rem;
  }
  .platform-row{ display:flex; flex-wrap:wrap; gap:.6rem; margin-top:1.4rem; }
  .platform-row a{
    background:var(--blush); border:1.5px solid var(--petal);
    padding:.55rem 1rem; border-radius:999px; text-decoration:none;
    color:var(--wine); font-weight:700; font-size:.85rem;
  }
  .platform-row a:hover{ background:var(--petal-soft); }
  @media (max-width:720px){ .music-card{ grid-template-columns:1fr; } }

  /* ---------- PORTFOLIO ---------- */
  .perf-list{ margin-top:2.5rem; display:flex; flex-direction:column; gap:1.5rem; }
  .portfolio-collage{
    margin-top:1.8rem; width:100%; max-width:520px;
    border-radius:18px; box-shadow:var(--shadow);
  }
  .perf-row{
    display:grid; grid-template-columns:170px 1fr; gap:1.5rem;
    background:#fff; border-radius:18px; box-shadow:var(--shadow); overflow:hidden;
    align-items:stretch;
  }
  .perf-row img{ width:100%; height:100%; object-fit:cover; }
  .perf-text{ padding:1.4rem 1.4rem 1.4rem 0; }
  .perf-text .date{ font-size:.78rem; text-transform:uppercase; letter-spacing:.12em; color:var(--crimson-deep); font-weight:700; }
  .perf-text h3{ margin:.25rem 0 .3rem; font-size:1.25rem; }
  @media (max-width:600px){ .perf-row{ grid-template-columns:1fr; } .perf-text{ padding:0 1.2rem 1.2rem; } }

  /* ---------- SHOP ---------- */
  .shop-box{
    margin-top:2rem; background:#fff; border-radius:24px; padding:3rem 2rem;
    text-align:center; box-shadow:var(--shadow);
  }
  .shop-box .ic{ font-size:2.4rem; }

  /* ---------- CONTACT ---------- */
  .contact-wrap{
    display:grid; grid-template-columns:.9fr 1.1fr; gap:3rem; align-items:center;
  }
  .contact-wrap .halo-frame{ width:min(320px,80vw); margin:0 auto; }
  .contact-msg{ font-size:1.15rem; line-height:1.7; }
  @media (max-width:820px){ .contact-wrap{ grid-template-columns:1fr; } }

  footer{
    text-align:center; padding:2.2rem 1rem 3rem; font-size:.8rem; color:var(--wine); opacity:.6;
  }

  @media (prefers-reduced-motion: reduce){
    *{ animation-duration:.001ms !important; animation-iteration-count:1 !important; transition:none !important; scroll-behavior:auto !important; }
  }
</style>
</head>
<body>
<div aria-hidden="true" class="sparkle-field" id="sparkleField"></div>
<header>
<div class="nav-wrap">
<a class="brand" href="#home" onclick="showTab('home')"><img alt="" class="brand-logo" data-img="logo"/>ABSOLUTE AiDOL</a>
<nav id="tabNav">
<button data-tab="home">Home</button>
<button data-tab="socials">Socials</button>
<button data-tab="artist">Artist</button>
<button data-tab="music">Music</button>
<button data-tab="portfolio">Portfolio</button>
<button data-tab="shop">Shop</button>
<button data-tab="contact">Contact</button>
</nav>
</div>
</header>
<main>
<div class="wrap">
<!-- ============ HOME ============ -->
<section class="panel" id="home">
<div class="home-hero">
<div class="hero-photo-wrap">
<div class="winged-frame">
<div class="halo-frame">
<img alt="ABSOLUTE AiDOL smiling in a red and white idol outfit with devil horns" data-img="mainPortrait"/>
</div>
</div>
<div class="float-polaroid p1"><img alt="ABSOLUTE AiDOL close-up, playful claw-hands pose" data-img="portraitClose"/></div>
<div class="float-polaroid p2"><img alt="ABSOLUTE AiDOL performing on stage" data-img="gallery1"/></div>
<div class="float-polaroid p3"><img alt="ABSOLUTE AiDOL looking over her shoulder, smiling" data-img="portrait2"/></div>
</div>
<div class="home-copy">
<p class="eyebrow">Seattle · Indie J-Pop Idol</p>
<h1 class="name">ABSOLUTE AiDOL</h1>
<p class="tagline">
<!-- EDIT ME: swap in your own one-line intro if you'd like something different -->
            Halo on, horns out! I'm an indie J-Pop idol and content creator bringing idol
            culture to new stages, new fans, and new corners of the internet, one performance at a time.
          </p>
<a class="btn" href="#artist" onclick="showTab('artist')">Meet the idol ♡</a>
<div class="booking-line">
            For booking or collaboration inquiries, contact
            <a href="mailto:absolute.aidol.staff@gmail.com">absolute.aidol.staff@gmail.com</a>
</div>
</div>
</div>
</section>
<!-- ============ SOCIALS ============ -->
<section class="panel" id="socials">
<p class="eyebrow">Find me everywhere</p>
<h2>Socials</h2>
<p class="lede">All my platforms live in one place! Tap below to follow along, or pick a platform directly.</p>
<div class="socials-main">
<p style="font-family:'Baloo 2',cursive; font-weight:700; font-size:1.2rem; margin-bottom:1rem;">
          Every link, one tap ♡
        </p>
<a class="btn" href="https://beacons.ai/absolute.aidol" rel="noopener" target="_blank">Open my Beacons page</a>
</div>
<!-- EDIT ME: once you have direct profile URLs, swap the href="#" below for each one -->
<div class="social-grid">
<a class="social-card" href="#" rel="noopener" target="_blank"><span class="ic">📷</span> Instagram</a>
<a class="social-card" href="#" rel="noopener" target="_blank"><span class="ic">🎵</span> TikTok</a>
<a class="social-card" href="#" rel="noopener" target="_blank"><span class="ic">▶️</span> YouTube</a>
<a class="social-card" href="#" rel="noopener" target="_blank"><span class="ic">🐦</span> X / Twitter</a>
</div>
</section>
<!-- ============ ARTIST ============ -->
<section class="panel" id="artist">
<div class="artist-hero">
<div class="winged-frame">
<div class="halo-frame">
<img alt="ABSOLUTE AiDOL portrait" data-img="portrait2"/>
</div>
</div>
<div>
<p class="eyebrow">The Artist</p>
<h2>About ABSOLUTE AiDOL</h2>
<!-- EDIT ME: expand this with your own story, influences, and milestones -->
<p class="lede">
            ABSOLUTE AiDOL is a Seattle-based indie idol and content creator dedicated to
            introducing new audiences to idol culture. Blending the sparkle of J-Pop with a
            community-first mindset, she performs at conventions across the U.S. and creates
            content that welcomes underrepresented fans into a scene that hasn't always felt
            built for them.
          </p>
<p class="lede" style="margin-top:1rem;">
            Her debut single marks the first chapter of a bigger story, one built on original
            music, high-energy performance, and a heart wide open to every fan who finds her.
            An EP and international performances are on the horizon.
          </p>
</div>
</div>
<div class="gallery">
<img alt="ABSOLUTE AiDOL, full outfit, hands on hips" data-img="mainPortrait"/>
<img alt="ABSOLUTE AiDOL performing on stage" data-img="gallery1"/>
<img alt="ABSOLUTE AiDOL kneeling on stage with microphone" data-img="gallery2"/>
<img alt="ABSOLUTE AiDOL close-up, playful pose" data-img="portraitClose"/>
</div>
</section>
<!-- ============ MUSIC ============ -->
<section class="panel" id="music">
<p class="eyebrow">Now Releasing</p>
<h2>Music</h2>
<div class="music-card" style="margin-top:1.5rem;">
<div class="music-cover">
<img alt="AiDOL WARS! single cover art" data-img="singleArt" style="width:260px;"/>
</div>
<div class="music-info">
<span class="release-pill">Releasing 4.17 · Streaming everywhere</span>
<h3 class="single-title">AiDOL WARS!</h3>
<p class="lede">My debut original single is almost here! Pre-save now so it lands
            straight in your library the moment it drops.</p>
<div style="margin-top:1.2rem;">
<!-- EDIT ME: swap in your pre-save link -->
<a class="btn" href="#" rel="noopener" target="_blank">Pre-save AiDOL WARS! ♡</a>
</div>
<div class="platform-row">
<!-- EDIT ME: swap these for your direct platform links once live -->
<a href="#" rel="noopener" target="_blank">Spotify</a>
<a href="#" rel="noopener" target="_blank">Apple Music</a>
<a href="#" rel="noopener" target="_blank">Pandora</a>
</div>
</div>
</div>
</section>
<!-- ============ PORTFOLIO ============ -->
<section class="panel" id="portfolio">
<p class="eyebrow">On Stage</p>
<h2>Portfolio</h2>
<p class="lede">A running list of performances, appearances, and sets. Replace the entries below with your own lineup.</p>
<img alt="Collage of ABSOLUTE AiDOL performing on stage" class="portfolio-collage" data-img="collage"/>
<!-- EDIT ME: replace each row's image, title, location, and date with real performances -->
<div class="perf-list">
<div class="perf-row">
<img alt="Performance photo" data-img="gallery1"/>
<div class="perf-text">
<p class="date">Convention Name · City, State</p>
<h3>Set title or performance name</h3>
<p class="lede">A line or two about the set, the crowd, or what made it special.</p>
</div>
</div>
<div class="perf-row">
<img alt="Performance photo" data-img="gallery2"/>
<div class="perf-text">
<p class="date">Convention Name · City, State</p>
<h3>Set title or performance name</h3>
<p class="lede">A line or two about the set, the crowd, or what made it special.</p>
</div>
</div>
<div class="perf-row">
<img alt="Performance photo" data-img="portrait2"/>
<div class="perf-text">
<p class="date">Convention Name · City, State</p>
<h3>Set title or performance name</h3>
<p class="lede">A line or two about the set, the crowd, or what made it special.</p>
</div>
</div>
</div>
</section>
<!-- ============ SHOP ============ -->
<section class="panel" id="shop">
<p class="eyebrow">Support</p>
<h2>Shop</h2>
<div class="shop-box">
<p class="ic">☕</p>
<p class="lede" style="margin:0 auto 1.4rem; text-align:center;">
          Every coffee helps fund costumes, travel, and new music. Thank you for the support! ♡
        </p>
<!-- EDIT ME: swap in your Ko-fi link -->
<a class="btn" href="https://ko-fi.com/" rel="noopener" target="_blank">Visit my Ko-fi</a>
</div>
</section>
<!-- ============ CONTACT ============ -->
<section class="panel" id="contact">
<div class="contact-wrap">
<div class="winged-frame">
<div class="halo-frame">
<img alt="ABSOLUTE AiDOL portrait" data-img="portrait2"/>
</div>
</div>
<div>
<p class="eyebrow">Get In Touch</p>
<h2>Contact</h2>
<p class="contact-msg">
            I look forward to working with you! Please reach out to me for any work,
            collaborations, or performance requests!
          </p>
<div class="booking-line">
            For booking, work, or collaboration inquiries, email
            <a href="mailto:absolute.aidol.staff@gmail.com">absolute.aidol.staff@gmail.com</a>
</div>
<a class="btn ghost" href="mailto:absolute.aidol.staff@gmail.com" style="margin-top:1.4rem;">Email me directly</a>
</div>
</div>
</section>
</div>
</main>
<footer>
  ABSOLUTE AiDOL · made with ♡ in Seattle
</footer>
<script>
  const IMAGES={logo:"https://i.imgur.com/YOURLOGO.png",mainPortrait:"https://i.imgur.com/MAINPORTRAIT.jpg",portrait2:"https://i.imgur.com/PORTRAIT2.jpg",portraitClose:"https://i.imgur.com/CLOSEUP.jpg",gallery1:"https://i.imgur.com/GALLERY1.jpg",gallery2:"https://i.imgur.com/GALLERY2.jpg",collage:"https://i.imgur.com/COLLAGE.jpg",singleArt:"https://i.imgur.com/SINGLEART.png",wing:"https://i.imgur.com/WING.png"};
document.documentElement.style.setProperty("--wing-image",`url(${IMAGES.wing})`);
document.querySelectorAll("[data-img]").forEach(img=>{const k=img.dataset.img;if(IMAGES[k]) img.src=IMAGES[k];});

const tabs = ['home','socials','artist','music','portfolio','shop','contact'];
  const buttons = document.querySelectorAll('#tabNav button');

  function showTab(name){
    if(!tabs.includes(name)) name = 'home';
    document.querySelectorAll('.panel').forEach(p => p.classList.remove('active'));
    document.getElementById(name).classList.add('active');
    buttons.forEach(b => b.classList.toggle('active', b.dataset.tab === name));
    history.replaceState(null, '', '#' + name);
    window.scrollTo({top:0, behavior:'instant' in window ? 'instant' : 'auto'});
  }

  buttons.forEach(b => b.addEventListener('click', () => showTab(b.dataset.tab)));

  window.addEventListener('DOMContentLoaded', () => {
    const initial = location.hash ? location.hash.replace('#','') : 'home';
    showTab(initial);

    // ambient sparkles
    const field = document.getElementById('sparkleField');
    const glyphs = ['♡','✦','☆'];
    for(let i=0;i<14;i++){
      const s = document.createElement('span');
      s.className = 'sparkle';
      s.textContent = glyphs[Math.floor(Math.random()*glyphs.length)];
      s.style.left = Math.random()*100 + 'vw';
      s.style.animationDuration = (12 + Math.random()*10) + 's';
      s.style.animationDelay = (Math.random()*14) + 's';
      s.style.fontSize = (0.8 + Math.random()*0.9) + 'rem';
      field.appendChild(s);
    }
  });
</script>
</body>
</html>
